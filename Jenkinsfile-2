pipeline {
    agent any
    
   stage('Print Workspace') {
    steps {
        sh 'pwd'
    }
}

    stages {
        stage('Deploy') {
            steps {
                sh 'ansible-playbook -i hosts.txt playbook3.yml'
            }
        }
    }
}
